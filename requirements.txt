# Runtime dependencies for the pyramidElevatorDist PACKAGE LOGIC only
# (segmentation + prediction). Mirrors [project.dependencies] in the root
# pyproject.toml —
# nothing here pulls in the UI, reports, data loaders, or dev tooling.
#
# For everything the full project needs, use requirements-ui.txt.
numpy>=1.24
pandas>=2.0
scipy>=1.10
pydantic>=2.0
