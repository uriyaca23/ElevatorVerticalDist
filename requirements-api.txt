# Runtime deps for the FastAPI service (api/).
# Keep in sync with api/Dockerfile.
numpy>=1.24
pandas>=2.0
scipy>=1.10
pydantic>=2.0
fastapi>=0.110
uvicorn[standard]>=0.30
# Pulled in transitively via src/segmentation/.../fit_elevator_parameters/common.py
# (it imports matplotlib at module-top for plotting helpers — lazy-importing
# would be cleaner but invasive; carrying ~50 MB of matplotlib in the API
# image is the minimum-blast-radius fix).
matplotlib>=3.7
