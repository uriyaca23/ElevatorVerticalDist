"""Pure trapezoid-fit primitives shared by the template-match detector.

The offline grid fitters and their plotting/IO live in the application
repo (``src/segmentation/.../fit_elevator_parameters/``); only the
signal-processing helpers and fit dataclasses ship here.
"""
