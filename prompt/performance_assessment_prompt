I need to do some prep work to enable generating statistical analysis for a different dataset that I have (lets call it "work dataset")
Essentially, what I want is to be able to quantify how reliable the results of the ZUPT algorithm is using 90% confidence intervals as long as rejection (algorithm can reject a sample if for example someone shakes the phone inside the elevator and it makes the ZUPT algorithm unreliable)

The way to do this should involve both theory (maybe add inside the code some categories of accelerometer data that is incompatible with a reliable algorithm output) as well as practical data (my dataset contains several measurements of elevator rides, where the work dataset is segmented into each example, where each example has an accelerometer measurement file as well as a gt metadata file that also includes elevator vertical travel height).

The theory should be used to hard code some cases where the algorithm is unreliable and the practical data should be used to create statistically reliable 90% confidence intervals. The theory can also be used to create confidence intervals, but these need to be very reliable if they don't use actual real data and only theory, because sometimes in reality there are other factors at play that we cannot plan. for the theory, you can go ahead and visit the camera orientation repository:

https://github.com/uriyaca23/CameraOrientation

this repository under https://github.com/uriyaca23/CameraOrientation/blob/master/core/noise_db.py contains the noise databases for the accelerometer sensor for all modern phones, so when someone gives you a phone model and an accelerometer recording you should have all the information you need in order to estimate 90% confidence interval of the vertical height traveled estimation of the ZUPT algorithm.

For now, we don't yet have access to this work dataset, so for now you can try to build an example format for an example dataset that applies the things you said, and run all of your code\tests on it to make sure that you did everything properly. Note that I want this to be easy and plug and play for me once I actually use the work dataset-

there should be a clear script I need to run as well as clear python requirements for it to work, and it should naturally generate a tool I can use both to get predictions and get 90% confidence intervals for future samples. the data in this folder can be used to calibrate the 90% confidence interval, but there should also be a "test" folder where I don't have GT of the results and the algorithm should output the prediction and uncertainty of the algorithm.

Please also add to the "tests" folder of this project multiple different tests for all aspects of this task to make sure that everything works so that when you finish you have the perfect project code.

to summarize:
1. build a synthetic dataset that emulates the dataset I have internally at my work. make it as realistic as possible and varied in terms of the kind of effects we expect to see in real data. Seprate it into "train" where we have the gt and "test" where we don't have the gt (but do have the name of the phone model)
2. write code that generates decline\accept for the prediction as well as 90% confidence intervals in the case that it is accepted
3. use theoretical methods for this. Run these methods on the synthetic dataset to ensure that you indeed have 90% confidence. Iterate multiple times on your code to fine tune the algorithm until it is indeed effective and valid.
4. Improve upon the theoretical methods by running code that takes the data from the dataset and generates empricial 90% confidence using a suitable method (maybe a conformer? you can debate and decide on the best method yourself). Note that the difference from the third step is that here we actually use the data to "learn" the confidence, in the third step we only use it as validation of how good the theory is.
5. Write an effective and easy to use script that has a learning step on the train data and then generates prediction on the test data. add it to the readme, explain how it works, how to prepare the dataset, and how to run it