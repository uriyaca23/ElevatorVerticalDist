I have the following task- using accelerometer data only, and given that an accelerometer recording is of a phone inside an elevator, estimate the height difference accrued during this elevator sequence.

For this task, we will use the ADVIO dataset https://github.com/AaltoVision/ADVIO/tree/master
the files themselves are already downloaded, but you can pull the git to gain access to the readme, scripts, ect.
Inside the papers folder there is a paper about the ADVIO dataset that you should read to gain deep understanding of the dataset.

Since the ADVIO dataset contains many kinds of sequences and we are only interested in elevator segments, the first thing you should do is either find online a detailed tagging of the elevator segments or just manually look at the video of each segment to find the exact start and end times of each elevator segment. This should then be maticioulisly documented into a metadata file that you can use for the rest of the project. Make sure that the timestamps are correctly calibrated with the measurements and use test scripts to double check yourself and make sure that you did not make any mistakes on this task. note that all test scripts are saved into the tests folder.

Secondly, after we have all of the time segments of elevator segments, you should research online for 3 main algorithms that can be used to estimate the vertical distance traveled upwards inside an elevator using only accelerometer data. Implement all three algorithms and note their pros and cons in detail into a word document file. Review your theory, look for online papers, save them into the papers folder, and be sure to do this very seriously as this is a very important part of the job. Add tests that make sure that all three codes actually work as intended for a few simple example measurements. iterate on the code until it works flawlessly as required for all three implementations

Thirdly, now it's time to actually run the 3 algorithms on the dataset for the elevator segments that you exctracted in the first part. Use the algorithms to estimate the height difference and then compare the results to the height derived from the barometer as well as the height from the gt (they used floor plans, point clouds, ect... in the gt to get accurate estimates of the heights of each floor. make sure you carefully study how this was done)

Fourthly, compare the results of all the different algorithms, and iterate on each one of them multiple times until they are perfect. Also, run test scripts that ensure that the results are reasonable and there are no bugs for any of the edge cases.

Fifthly, now it's time to write a detailed work document that details all of the dataset, work, progress, iterations, results, ect. All of this should be professional grade, long and detailed (50 pages long at least) and must contain as images the visualization of the accuracy of the results, both of specific example segments as well as summary of the results of all of the segments. This should be an academic level research paper. make sure to use python inline OMML for word such that you can actually properly compile inline word equations inside the word document, ect... This must be very professional. Add tests that open the word file and visually look at it to make sure everything is correctly saves\inputted without errors.

Also, copy this prompt and save it. For each work you do, for each segment, you must read this prompt again to ensure that you do not miss anything. this must be hard coded deeply into everything you do so that I won't need to remind you again and again to adhere to all of this work and rules.