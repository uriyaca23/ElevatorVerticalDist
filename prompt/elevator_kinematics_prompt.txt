Let's start working on a large research task.
Please read the docs\Research_Report.docx to get aquainted with the research done so far. Here is the new research idea and what needs to get done:

1. Create a new research report document and put all of the work you've done in all the steps of this prompt into this research report. we will now detail what will be inside this document.

2. read the elevator kinematics papers under the papers folder. Understand in detail how elevators work. understand the constraints they work under and do dilligent research about the type of elevators vastly used today for residential and commerccial buildings. analyze and compare the different kinematics models for elevators- the trapezoid, 7 steps, cycloid, 7 degree polynomial, ect.. I believe that only the steps one is actually vastly used (maximum jerk, maximum acc, maximum vel) but you should research on your own to confirm\deny this. Please also do research about commom height differences between different floors. Summarize all of this into the report as background research and modeling the problem ect. Into this modeling also heavily investigate the way that humans behave with their phone- taking phone to their pocket, their hand, how this translates to noise, how this can be detected\not detected if we have only accelerometer data (which effectively we only measure accelerometer magnitude) vs if we have accelerometer and orientation data (which means that we now have 3d accelerometer measurement where we can more easily cancel the 2 translational direction noises which improves the prediction). Do this extremely professionaly into the document, draw inspiration from the Research report on the format\style that we want for the report. this is essentially the theory part

3. Now for algorithmic theory- in the research report we used ZUPT for the height difference estimator. However, what I realized is that if we know we are in an elevator, the shape of the elevator pulse is highly predictable and dependent on just a few paraemters of the elevator- like maximum velocity, maximum acceleration, maximum jerk, and this distance traveled\travel time which is essentially actually the thing that we want to estimate. if we know all of these values then we know the shape of the pulse exactly. so essentially maybe the algorithm should simply be an optimal ideal estimator for these quantities and if we can do this well we can simply solve the entire quality + height difference estimation. Note that while the max vel and max acc and max jerk are of course joint and repeatable for segments of the same building, here we want to do this for each segment individually and not use information from other segments to help us estimate these quantities. also not the important cases where we maybe only move 1 floor and in that case the start and end pulses could overlap and we might not even reach the max acceleration and max velocity because we slow down fast enough because the distance is so small. This algorithm needs to be perfect and ideal- go into great effort to research the optimal way to solve this problem, analyze theory for problems of this kind to understand the best way to solve this. Present the algorithm in the report and explain it and prove why it's optimal and explain different examples where it is already used successfully. The output for the algorithm should essentially be- the travel distance, the 90% confidence interval for the travel distance (which should be theoretical, this is an estimator after all. Note that the confidence interval can be different for each segment- for easier segments we can have tighter interval and for harder segments we can have larger interval! we try to aim for the confidence to be small enough to focus us to the correct floor so like +-1.5m maximum ideally). Note that in addition to the travel distance and 90% confidence interval you can also reject\refuse to give prediction if the data is terrible or the predictions are bad, so you can have a quality score similiar to the thing we were aiming for in the previous research report.

important note- in the previous research report our algorithm assumed constant sampling rate. however we want the algorithm now to instead be given a generic timestep column which can have a non constant sampling rate but still be able to solve the problem using the combination of timesteps +  measurement values.

Suggest two different algorithms- one based purely on accelerometer and another that is based on accelerometer + orientation.

4. At this point we are already past the theoretical background and algorithmic theory for solving the problem. Now we need to fill in all the rest very similiarly to what was shown in the research report- we want all the graphs analyzing performance of the algorithm, same kind of format as the previous report, also attach the dataset analysis that you have already done in the beginning of the report, add new analysis of the results similiar to the format of the report, ect ect.... this needs to be a full report start to finish very similiar to the old research report. this time we do not try to perform the segmentation and detection problem though- we are purely focused solely on the quality and estimation problems and the segments themselves are done purely from the gt which was generated from the barometer. please read the table of contents from the previous report to properly understand the kind of format that we want for this new report. it's essentially the same just with a different algorithm and using this new prior knowledge about elevators. Please don't remove or delete necessary things like dataset background, research history (make sure to compare the results to previous iterations!), usage guide ect ect....

5. For better understanding about prior work that we did, please go into the prompts folder and read previous historical prompts that we have done to have a better understand about things that are important to us because I don't remember everything that I have previously told you but almost everything there is still the things that I care about and are important for me. Read them in detail and make sure to take them into account. Don't make me remind you again and again about things that are important to me