Okay. We are now beginning on a very major tasks. I will detail many steps but you must follow and read what I write slowly so that you don't forget anything. I am saving this prompt under prompts because it's very long and during your work you must always keep looking at it in order not to forget your task and what you need to do. Let's begin:

step 1- building our new dataset

I focused all of the datasets into the datasets folder. I created a new dataset called "bar ilan dataset"- this dataset is an experiment that was done in the building from my apartment, whose goal was to create a varied dataset of using the elevator, to be used in future steps both for elevator detection as well as elevator vertical distance traveled.

The building in question is a building with 16 floors- there is the ground floor "L" that corresponds with the street level "bar ilan street", then floors 0-15 (in the gramuskha they labeled as floors 1-16 so make sure not to confused.) The heights of every floor is clearly marked on the gramuska, to help you out and not confused you I saved "gramushka focused" that has a drawing of the heights of all floors. notice that between the ground floor L and floor 0 there is a much larger distance than between the other floors, that all have a constant height difference. as you can see from the drawing, the height of L is -2.6, the height of 0 is 15.8, and every floor above that is 300 cm above so 18.8, 21.8, ect....

In the experiment I did 2 distinct different examples- 1 part with the camera held in my hand, visibly able to see the elevator ect, similiar to the way the phone was held in the ADVIO dataset, and 1 part with the camera held in my pocket.
For each of the parts I did a part where I am going up as well as a part where I was going down, and also segments with long elevator travel distances and short elevator travel distances. I did this because I wanted a varied dataset. you can derive what part of the experiment each is easily from the camera, the audio, the elevator direction, phone pose, ect...

I created 2 recordings- 1 of a video and 1 of a sensor fusion app that records all of my sensors. note that for this entire project we are only allowed to use the accelerometer sensor for the vertical direction and elevator segment detection, but for building the gt\comparisons\visualizations\videos we are allowed to use the other sensors. Notice that since I had to manually start the camera\sensor the timesteps are not exactly synchronized for both sensors, so it is your job to also make sure they are fully synchronized and cut out the segments where only the video was active or only the sensors were active.

What I want you to do is please build a proper dataset that can be used in the future, together with the ADVIO dataset, to use as extra data for elevators since ADVIO is light on elevator instances. This should enable us to build and test both elevator vertical direction and elevator segment detection more easily. you are allowed to use the audio of the video (the elevator says out loud which floor we are) you may also use the barometer as well as the temperature (noted in the metadata.txt) to calculate the vertical height\whether we are inside an elevator or not (note that some segments I was just walking around outside the elevator, so these parts should be tagged "not elevator" where parts where I was inside the elevator should be tagged "elevator"). Please also note that the barometer is a sensor of it's own so it's not purely accurate for creating a height gt but it is useful for detecting whether we are at a stationary height or not, ect. As we saw in previous segments, we would like to compare the barometer derived height to the accelerometer derived height purely for comparison.

The organized dataset should have:

1. sensor csv files for each sensor so that they are properly seperated
2. a video file (note that tyou can reduce the quality to make it less heavy\easy for you to use and open in the future)
3. a metadata file that would be used as gt for the entire run. Should have 10 times per second a tag of all the information neccecary for us- like the height, tag of whether we are in an elevator or not, elevator segment instance (for example, going from floor 1 to 2 can be one continuous segment and then going from 3 to 5 can be another one if we didn't stop in any floors in between), also should have a tag of whether the phone was in the pocket or in the hand (can be derived from the video or the phone orientation sensor) and everything else that could be useful. Also make sure that segments where we are not in an elevator but just for example walking around a specific floor should also be segmented into a new number.

step 2- validating our datasets

After you finish building out the dataset in the required format, you need to run many tests to make sure you didn't make any errors. compare the heights to the barometer derived heights to see if they make sense, sample the timesteps of start\end elevator times where the phone was handheld to make sure that they are correctly identified, compare the audio cues when the phone was in the pocket, ect ect ect.... Please build as many tests and possible and you may not continue to the next segment until they all perfectly pass all of them. Please also add common sense tests based on your own logic that check consistency between all of the data (barometer, accelerometer, orientation, video, video audio, ect...) that can help empower you to find and identify and improve the dataset, until all of the GT is perfectly done and calibrated, If you make even a single error on the GT, it will be very very bad

step 3- incorporating a new algorithm

Now, we want to expand the current algorithm from purely detecting the vertical distance traveled inside an elevator using purely accelerometer to also detecting elevator segments from a pure accelerometer recording. 

Secondly,  you should research online for 3 main algorithms that can be used for this purpose of detecting and segmenting elevator time periods from accelerometer data. Implement all three algorithms. Review your theory, look for online papers, save them into the papers folder, and be sure to do this very seriously as this is a very important part of the job. Add tests that make sure that all three codes actually work as intended for a few simple example measurements, you can use our dataset for these small iterations. iterate on the code until it works flawlessly as required for all three implementations

Thirdly, now it's time to actually run the 3 algorithms on the bar ilan as well as the ADVIO dataset combined. Note that there are many elevator segments in the bar ilan dataset and many non elevator segments in the ADVIO dataset so this should give you the richness in data that you need in order to build and create a good detection algorithm. Compare your results to the GT from the dataset, run many tests to make sure that everything is consistent and works properly.

Fourthly, compare the results of all the different algorithms, and iterate on each one of them multiple times until they are perfect (aka, the result of the detected segment perfectly matches the real segment from the data). Also, run test scripts that ensure that the results are reasonable and there are no bugs for any of the edge cases.

step 4- combining all of the segments together
Now that we have the datasets, the algorithms, the conformal prediction, the generated documents, ect, we want to join all of them together in a few steps:

1. we need to create a fully built pipeline that segments our dataset into train\test
2. Run the best elevator segment detection algorithm as well as the best elevator vertical distance detection algorithm on this train\test split, where we use conformal prediction and only allowed to use the train split for the conformal "learning". Make sure to save the final "model" which essentially should be a function I can load that I give as input an accelerometer file and generates as output the segments and the distances with 90% containement. Also generate as we did before videos, graphs, examples, aggregated comparisons, ect.. that can be used to save to the final summary document (draw inspiration from images and graphs that we did in the past)
3. Run 4 extra comparisons, where we use the best elevator segment detection algorithm with the two worse vertical distance algorithm as well as the two worse elevator segment detection algorithms with the best vertical distance algorithm. Also generate as we did before videos, graphs, examples, aggregated comparisons, ect.. that can be used to save to the final summary document (draw inspiration from images and graphs that we did in the past)
4. Take all of the previous documents we wrote\made (both the final elevator height report as well as the ZUPT confidence interval report), join them together, expand with the elevator segment detection research, theory, practical results, algorithm comparison, analysis of results, ect, add on top of that the results from the joint run that does both vertical distance estimation as well as elevator segmentation and detection, create a full thorough report that contains all of that together (as well as analysis, commentary on results, ect...)

Run many tests on all of these steps to make sure that both the report is perfect, that you didn't delete\remove\not include important segments, that there are no fillers but real good content, and that we achieved good results.

step 5- final step
This step is the most important one- you must now read over everything you have done in all of the segments, and critically analyze yourself, your mistakes, and errors. You need to fully read the entire report, the entire code, the algorithms you chose, the dataset and how you built it, the GT and how it was generated, the quality of the dataset and the results of your algorithms, create a list of all questionable and dubious things that you have done, and create a check list to fix them, and repeat over and over again, including that you come to this step. Only once you did this step multiple times (2-3 times) and you didn't find any more errors, you may complete your task