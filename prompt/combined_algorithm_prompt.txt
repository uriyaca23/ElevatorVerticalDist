These results are not good enough. Firstly, we need to investigate whether or not we can use the accelerometer itself to get good enough estimates of the pitch and the roll which are the only angles we care about anyway when it comes to the height estimation. secondly, we also know that when we are in the elevator, while we can walk around in the elevator and change phone orientation, our travel direction is up or down, so this is a prior we can use for the estimator.

Another thing is that even if the accelerometer is not enough and we also need the gyroscope, in this case maybe we can use at least only the accelerometer in order to reject or accept a measurement to begin with. If the segment is clearly unreliable, aka we detect meaningful orientation change during calibration or during the elevator ride itself, maybe we can just use this information to reject the output to begin with.

Please be creative, and come up with solutions and not just problems. Use what you have effectively and properly. Also please think about the problem under a few criterion that you MUST solve and reach certain absoloute requirements for this algorithm to be effective. For this to be easier, let's set a few criterrion that you must solve:

1. The elevator detection and segmentation algorithm: must be reliable, aka when we detect an elevator segment it must really be an elevator segment, and it should seperate different floor rides from each other (aka, if we take the elevator from floor 1-3 without stopping at floor 2 the entire ride 1-3 should be detected. If we continue from 3 later to floor 7 without stopping in between, this should be detected as a seperate ride then the one from 1-3, ect...). This must be reliable, but you are allowed to reject detections when you are not sure. You are allowed to throw out problematic instances, of course if you throw out too much that is a problem, so try to make sure that it is sane and only done when really neccesary

2. Given an elevator segment that was detected, we now want to decide whether to reject the segment because the height difference estimator that we intend to use in the future is going to be unreliable, for any reason that may be (whether orientation changes, shaking, ect... anything that you noticed caused estimation errors that cannot be solved). This must be reliable, aka when we decide to predict it has to be for a segment that we can actually predict and doesn't ruin all of our other predictions by causing the conformal to become huge. You are allowed to throw out problematic instances, of course if you throw out too much that is a problem, so try to make sure that it is sane and only done when really neccesary

3. Given that we decided not to reject that segment, now we want to estimate the height difference as well as give 90% containment rate for that estimated height using conformal prediction on our dataset. This must be reliable and reasonable, when we give prediction we need to make sure the 90% containment is not larger than 1 meters, because in that case we failed in our task.

Notice that unlike the previous segment, here we don't care about strict "multi algorithm comparisons" ect, we just care about absoloute results. You can use your previous research to help you lock in on the best algorithms. You cannot finish your task until you reach these very clear objectives. once you finish, update the final combined report to contain and summarize all of your work, of course keeping the useful visualizatios we used in previous segments and expanding on them. In your final report, you must look at all 3 tasks and use visualizations\graphs\examples\aggregates\ect to prove that you really hit your goals and milestones for all three parts of the algorithm.